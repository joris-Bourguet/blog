{% extends 'base.html.twig' %}
{% block title %}
	{{title}}
{% endblock %}
{% block body %}
	{% for article in articles %}
		<article>
			<h2>
				{{article.title}}
			</h2>
			<p class="metadata">Créé à
				{{article.createdAt | date('d/m/Y') }}
				le
				{{article.createdAt | date('H:i') }}
				dans la catégorie test</p>
			<div class="content">
				<img src="{{article.image}}" alt="">
				<p>{{article.content}}</p>
				<a href="{{ path('article_details', {'id': article.id}) }}">En savoir plus</a>
			</div>
		</article>
	{% endfor %}
{% endblock %}
